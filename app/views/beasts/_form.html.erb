<%= form_for(@beast) do |f| %>
  <% if @beast.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@beast.errors.count, "error") %> prohibited this beast from being saved:</h2>

      <ul>
      <% @beast.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
